# 数据模型与结算规则（v0.1）

## 1. 核心实体

- `User`：微信身份、实名状态、角色、风险状态；
- `Product/Sku`：商品、规格、分类、售价、成本、上下架状态；
- `InventoryAccount`：`PUBLIC` 平台公共库存、`CONSIGNED` 分销员寄存库存；
- `InventoryLot`：批次、有效期、责任人、可用/预占/已出库数量；
- `Order/OrderItem`：订单、商品、价格快照、履约方式、推广归属快照；
- `Group`：团购、团长、截团时间、目标数量、状态；
- `ReferralRelation`：客户与推广员归属、来源、建立/撤销时间、审计信息；
- `AgentQualification`：课程凭证、有效集采金额、社群审核、激活/冻结原因；
- `CommissionLedger`：佣金预计、冻结、可结算、已结算、冲销；
- `SettlementBatch`：日结批次、计算版本、复核人、封存时间；
- `Withdrawal`：提现申请、风控、审核、打款回执；
- `OPCLedger`：OPC 主体经营流水和单据（建议二期）；
- `AuditLog`：所有人工和自动操作的前后值。

## 2. 订单状态机

`CREATED → PENDING_PAYMENT → PAID → FULFILLING → SHIPPED/PICKUP_READY → COMPLETED`

异常分支：`PENDING_PAYMENT → CANCELLED`、`PAID → REFUNDING → REFUNDED`、`SHIPPED → AFTER_SALES → REFUNDED/PARTIAL_REFUNDED`。

订单必须保存支付、履约、售后、结算四组状态，不能用一个字段混合表达。

## 3. 库存账原则

- 下单预占：`available -= qty; reserved += qty`；
- 取消/超时：释放预占；
- 出库：`reserved -= qty; shipped += qty`；
- 退货入库：按质检结果进入可售或损耗；
- 公共库存和寄存库存独立记账，禁止负库存；
- 每次变更生成不可变 `InventoryTransaction`，支持按订单、批次、操作人追溯。

## 4. 佣金计算（待法务确认）

仅作为系统**可配置试算**，上线前不得对外承诺：

```text
commission_base = 商品实际支付金额 - 退款金额 - 平台补贴 - 运费（参数化）
本人预估 = commission_base × 0.5%
直推预估 = commission_base × 0.3%
间推预估 = commission_base × 0.15%
团购直推预估 = commission_base × 团购直推比例
```

- 佣金比例按商品/活动版本快照写入订单；
- 只有资格状态 `ACTIVE` 且订单完成并过售后期后，才进入 `READY`；
- 退款订单不计入业绩；部分退款按退款比例冲销；
- 批次封存后禁止修改原记录，只能追加负向冲销记录；
- 日结只是批次频率，不代表支付成功后立即可提现；
- 团队流水口径、层级深度、月度奖励需客户书面确认和专项法务意见。

## 5. 资格校验

三项条件全部满足才允许佣金进入 `READY`：

1. 课程报名凭证已审核；
2. 有效集采金额达到客户确认门槛；
3. 社群资料审核通过。

任何条件失效时：新佣金进入 `FROZEN`，历史已结算记录不自动删除，需按审批流程处理。

## 6. 财务对账

每日生成：支付总额、退款总额、履约完成额、可结算佣金、冻结佣金、提现金额、平台服务费。对账差异必须有差异单、责任人和处理状态。
